		        //console.log(select);
		        //var data.valor    = jQuery(this).attr('data-valor');
		        //var data.texto    = jQuery(this).val();

		        //console.log(valor+' - '+texto);
		        //console.log(arreglo);



		        //var list   = [];
		        /*
		        $.getJSON(texto, function(data)
		        {
		            $.each(data, function(key, val)
		            {
		                list.push('<option value="' + val.id + '">' + val.name + '</option>');
		            });

		            //select.html(list.join(''));
		           // select.selectpicker('refresh');
		           console.log(list);
		        });
		        */


		        
  			 //optID += 1;
   			//console.log( jQuery('optgroup[valor="'+optgroupLabel+'"]').attr('label')  ); //.dropdown-header find('option').text() 
            //siblings(':first').html(); //.siblings('.dropdown-header').html();
            //sibblings().next(); //.replace(/\s+/g, '-').toLowerCase();
            //console.log(optgroupLabel);

              //console.log(valor);


            //jQuery('.niveles_detalles').text('selectedD: ' + selectedD + '  newValue: ' + isSelected + ' oldValue: ' + previousValue);
  

             // Get the opt group label
         /*
            var optgroupLabel = jQuery(this).find('option').eq(clickedIndex).attr('data-valor');
          

            $grupo = ( jQuery('optgroup[valor="'+optgroupLabel+'"]').attr('label') != undefined ) ? jQuery('optgroup[valor="'+optgroupLabel+'"]').attr('label') : ''; 

            console.log($grupo);

  			//este es el ultimo elemento cliqueado
  			let selectedD = jQuery(this).find('option').eq(clickedIndex).text(); 
  			//console.log(selectedD);	
            let valor=jQuery(this).val();

            let valor2=jQuery(this)[0].val(); //.find('option').eq(clickedIndex).attr('data-valor');
            console.log(valor2);
            
            nivel='';
            valor_anterior='';
            jQuery.each( valor, function( key, value ) {
              //console.log(value);
              if (valor_anterior!=value) {
              	  pp=jQuery('option[value="'+value+'"]').attr('data-valor');
	              nivel +='<div class="'+((key==0) ? "nivel-activo" : "") +'">';
	              nivel +='  <p>'+pp+'  <span class="fas fa-caret-right flecha-nivel"></span></p>';
	              nivel +=' </div>';
	              nivel +='<hr>';
	              valor_anterior==value ;

              }
              nivel +='<div class="'+((key==0) ? "nivel-activo" : "") +'">';
              nivel +='  <p>'+value+' <span class="fas fa-caret-right flecha-nivel"></span></p>';
              nivel +=' </div>';
              nivel +='<hr>';
            });
            
            jQuery('.niveles_detalles').children().remove();
            jQuery('.niveles_detalles').append(nivel);




            */